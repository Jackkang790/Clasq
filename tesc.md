# Codex AI 작업 지침

## 1. 작업 목적

기존 PySide6 기반 파일 정리 프로그램의 `organize_view`에 **「경로 삭제」 버튼을 추가**한다.

이 기능에서 말하는 "경로 삭제"는 **DB의 데이터를 삭제하는 기능이 아니다.**

사용자가 현재 `organize_view`에서 선택한 경로를 **이번 정리 대상에서 제외하는 기능**이다.

---

# 2. 경로 삭제 기능의 정확한 의미

### 중요

`경로 삭제` 버튼을 눌렀을 때:

```text
X file_manager.db 데이터 삭제
X managed_paths 데이터 삭제
X preset.json 데이터 삭제
X 실제 폴더/파일 삭제
```

를 수행하지 않는다.

오직 현재 `organize_view`에서 관리하고 있는 **정리 대상 목록에서 해당 경로를 제거**한다.

즉:

```text
현재 정리 대상
├─ D:\새 폴더
├─ D:\Project
└─ D:\Images
```

에서 사용자가 `D:\Project`를 선택하고 `경로 삭제`를 누르면:

```text
현재 정리 대상
├─ D:\새 폴더
└─ D:\Images
```

로 변경된다.

`D:\Project`라는 실제 경로 자체는 삭제되지 않는다.

---

# 3. 버튼 위치

기존 `organize_view`의 버튼 배치를 다음과 같이 변경한다.

### 변경 전

```text
[프리셋 불러오기] [경로 추가] [자동정리]
```

### 변경 후

```text
[프리셋 불러오기] [경로 추가] [경로 삭제] [자동정리]
```

**최종 버튼 순서는 반드시 다음과 같아야 한다.**

```text
[프리셋 불러오기] [경로 추가] [경로 삭제] [자동정리]
```

`경로 삭제` 버튼은 기존 `경로 추가` 버튼의 **바로 오른쪽**, `자동정리` 버튼의 **바로 왼쪽**에 배치한다.

---

# 4. 버튼 이름 변경

기존 버튼의 명칭도 확인하여 다음과 같이 통일한다.

```text
프리셋 불러오기
경로 추가
경로 삭제
자동정리
```

최종적으로 사용자에게 표시되는 버튼은:

```text
[프리셋 불러오기] [경로 추가] [경로 삭제] [자동정리]
```

가 되어야 한다.

기존 코드에서 `경로 추가하기`, `자동정리하기` 등의 이름을 사용하고 있다면 이번 요구사항에 맞게 버튼 표시 이름을 변경한다.

단, **내부 함수명이나 클래스명은 기존 코드 구조를 불필요하게 변경하지 않는다.**

---

# 5. 디자인

`경로 삭제` 버튼의 디자인은 기존 `organize_view`의 버튼을 새로 디자인하지 않는다.

특히 **`setting_view`에 존재하는 기존 버튼 디자인을 참조하여 동일한 스타일로 구현한다.**

참조 대상:

```text
setting_view
└─ 기존 버튼 디자인
```

다음 요소를 최대한 동일하게 맞춘다.

* 버튼 크기
* 버튼 높이
* 버튼 폭
* 폰트
* 폰트 크기
* 글자 스타일
* 배경색
* 글자색
* Border
* Border Radius
* Hover 효과
* Pressed 효과
* Disabled 효과
* Padding
* 버튼 간격

즉, `setting_view`의 기존 버튼 스타일을 확인한 후 `organize_view`의 `경로 삭제` 버튼에 적용한다.

---

# 6. 삭제 대상 선택

`경로 삭제` 기능은 현재 `organize_view`에 표시되어 있는 **정리 대상 목록에서 선택된 항목을 제거**하는 방식으로 구현한다.

예:

```text
정리 대상

☑ D:\새 폴더
☑ D:\Project
☑ D:\Images
```

사용자가 `D:\Project`를 선택하고:

```text
[경로 삭제]
```

를 누르면:

```text
정리 대상

☑ D:\새 폴더
☑ D:\Images
```

가 된다.

---

# 7. 여러 경로 선택

기존 `organize_view`의 경로 목록이 여러 항목을 선택할 수 있도록 구현되어 있다면 기존 선택 방식을 그대로 따른다.

여러 경로가 선택된 경우:

```text
D:\Project
D:\Images
D:\Test
```

모두 현재 정리 대상에서 제거한다.

단, 실제 파일이나 폴더는 삭제하지 않는다.

---

# 8. 선택된 경로가 없는 경우

사용자가 아무 경로도 선택하지 않은 상태에서:

```text
[경로 삭제]
```

를 클릭했을 경우 프로그램이 오류를 발생시키지 않도록 한다.

기존 프로젝트에 메시지 Dialog 처리 방식이 있다면 그 방식을 사용하여 사용자에게 다음과 같이 알릴 수 있다.

```text
삭제할 경로를 선택해주세요.
```

또는 기존 UI의 안내 방식에 맞춰 처리한다.

---

# 9. DB 데이터 보존

이번 기능에서 가장 중요한 사항이다.

`경로 삭제`를 수행하더라도 DB 데이터에는 영향을 주지 않는다.

예를 들어 DB 또는 기타 저장 데이터에:

```text
D:\Project
```

가 저장되어 있더라도 `organize_view`에서 삭제했다고 해서 해당 데이터를 DB에서 삭제하지 않는다.

즉:

```text
[경로 삭제]
      ↓
현재 organize_view 정리 대상에서만 제거
      ↓
DB 데이터 유지
```

이다.

---

# 10. 프리셋과의 관계

`프리셋 불러오기`를 통해 가져온 경로 역시 현재 정리 대상에 추가된 이후에는 일반적인 정리 대상과 동일하게 취급한다.

예:

```text
[프리셋 불러오기]
        ↓
테스트 프리셋 선택
        ↓
D:\새 폴더
D:\Project
D:\Images
        ↓
organize_view 정리 대상에 표시
```

이후:

```text
D:\Project 선택
        ↓
[경로 삭제]
        ↓
현재 정리 대상에서 D:\Project 제거
```

한다.

단, `preset.json`에 저장된 원본 프리셋 데이터는 수정하지 않는다.

다음 데이터는 그대로 유지한다.

```text
assets/preset.json
```

즉:

```text
프리셋 원본
    ↓
organize_view에 불러오기
    ↓
일부 경로 삭제
    ↓
현재 정리 대상에서만 제외
```

이다.

---

# 11. 자동정리 기능과의 관계

`경로 삭제`로 제거된 경로는 이후 `자동정리`를 실행할 때 정리 대상에 포함되지 않아야 한다.

예:

```text
현재 정리 대상

D:\새 폴더
D:\Project
D:\Images
```

에서:

```text
D:\Project
```

를 삭제한 후:

```text
[자동정리]
```

를 실행하면 자동정리 대상은:

```text
D:\새 폴더
D:\Images
```

만 포함해야 한다.

`D:\Project`에 대해서는 자동정리를 수행하지 않는다.

---

# 12. 실제 파일 시스템에는 영향을 주지 않음

`경로 삭제`라는 이름 때문에 실제 Windows 파일/폴더를 삭제하는 기능으로 오해할 수 있으므로 반드시 다음 원칙을 지킨다.

### 수행하지 않는 작업

```text
X os.remove()
X shutil.rmtree()
X 실제 폴더 삭제
X 실제 파일 삭제
X DB DELETE
X preset.json 수정
```

### 수행하는 작업

```text
O 현재 organize_view의 정리 대상 목록에서 제거
```

---

# 13. 기존 코드 우선 분석

코드를 수정하기 전에 현재 `organize_view`의 구조를 먼저 확인한다.

특히 다음을 확인한다.

```text
organize_view
├─ 프리셋 불러오기
├─ 경로 추가
├─ 자동정리
├─ 정리 대상 목록
└─ 경로 선택 처리
```

그리고 `setting_view`에서 버튼 디자인을 확인한다.

```text
setting_view
└─ 기존 버튼 Style / StyleSheet
```

가능하면 기존 경로 목록에서 사용하는:

* Model
* List Widget
* Table Widget
* Selection Model
* 데이터 구조

를 그대로 활용한다.

새로운 데이터 구조를 불필요하게 만들지 않는다.

---

# 14. 기존 기능 유지

이번 작업으로 기존 기능을 삭제하거나 변경하지 않는다.

특히 다음 기능을 유지한다.

```text
[프리셋 불러오기]
[경로 추가]
[자동정리]
```

여기에:

```text
[경로 삭제]
```

기능만 추가한다.

최종 UI:

```text
[프리셋 불러오기] [경로 추가] [경로 삭제] [자동정리]
```

---

# 15. 최종 체크리스트

## UI

* [ ] `organize_view`에 `경로 삭제` 버튼이 추가되었는가?
* [ ] 버튼 순서가 정확히 `[프리셋 불러오기] [경로 추가] [경로 삭제] [자동정리]`인가?
* [ ] `경로 삭제`가 `경로 추가` 바로 오른쪽에 있는가?
* [ ] `경로 삭제`가 `자동정리` 바로 왼쪽에 있는가?
* [ ] 버튼 이름이 정확히 `경로 삭제`인가?
* [ ] `setting_view`의 기존 버튼 디자인을 참조했는가?
* [ ] 기존 UI 스타일과 자연스럽게 통일되는가?

## 경로 삭제

* [ ] 선택한 경로가 현재 `organize_view`의 정리 대상에서 제거되는가?
* [ ] 여러 경로를 선택한 경우 모두 제거되는가?
* [ ] 선택하지 않은 경우 오류가 발생하지 않는가?
* [ ] 삭제 후 정리 대상 목록이 즉시 갱신되는가?

## 데이터 보존

* [ ] `file_manager.db`의 데이터가 삭제되지 않는가?
* [ ] `managed_paths` 데이터가 삭제되지 않는가?
* [ ] `assets/preset.json`이 수정되지 않는가?
* [ ] 프리셋 원본 데이터가 변경되지 않는가?
* [ ] 실제 파일이 삭제되지 않는가?
* [ ] 실제 폴더가 삭제되지 않는가?

## 자동정리

* [ ] `경로 삭제`로 제외한 경로가 자동정리 대상에서도 제외되는가?
* [ ] 남아 있는 경로만 자동정리 대상이 되는가?
* [ ] 기존 자동정리 기능이 정상적으로 동작하는가?

## 기존 기능

* [ ] 프리셋 불러오기가 기존처럼 작동하는가?
* [ ] 경로 추가가 기존처럼 작동하는가?
* [ ] 자동정리가 기존처럼 작동하는가?
* [ ] 기존 `organize_view` UI가 불필요하게 변경되지 않았는가?

깃 테스트