## 작업 목적

현재 프로그램에서 확인된 다음 3가지 문제를 수정한다.

1. **검색 결과 클릭 시 해당 폴더가 열리지 않고 `내 문서`가 열리는 문제**
2. **DB에서 데이터를 삭제할 때 실제 파일까지 삭제되는 문제**
3. **파일 제목을 이용한 태깅으로 인해 검색 및 폴더 분류가 잘못되는 문제**

기존 기능과 UI는 최대한 유지하면서 문제의 원인을 수정한다.

---

# 1. 검색 결과 클릭 시 잘못된 폴더가 열리는 문제

## 현재 문제

검색하기 기능에서 검색 결과를 클릭하면 해당 파일 또는 폴더가 위치한 경로가 열려야 한다.

현재는 검색 결과를 클릭했을 때 해당 경로가 아닌 **`내 문서`가 열리는 문제가 발생**하고 있다.

### 현재 동작

```text
검색하기
    ↓
검색 결과 표시
    ↓
검색 결과 클릭
    ↓
내 문서 열림 ❌
```

### 원하는 동작

```text
검색하기
    ↓
검색 결과 표시
    ↓
검색 결과 클릭
    ↓
해당 파일/폴더의 실제 경로 열기
    ↓
정상적인 탐색기 위치 표시 ✅
```

## 수정 요구사항

검색 결과를 클릭했을 때 검색 결과 데이터에 저장되어 있는 **실제 `path`를 기준으로 경로를 열어야 한다.**

예:

```text
D:\새 폴더\application.yml
```

을 검색 결과로 클릭했다면:

```text
D:\새 폴더
```

가 Windows 탐색기로 열려야 한다.

폴더 자체가 검색 결과라면 해당 폴더를 직접 열어야 한다.

## 확인해야 할 부분

다음 부분을 우선 확인한다.

- 검색 결과에 실제 경로가 정상적으로 저장되어 있는지
- 검색 결과 클릭 이벤트에서 어떤 경로를 전달하는지
- 경로가 비어 있을 경우 기본 경로로 `내 문서`를 사용하는 코드가 있는지
- 상대 경로와 절대 경로를 잘못 처리하고 있는지
- Windows 탐색기 실행 로직이 올바른 경로를 전달받는지

특히 **검색 결과 클릭 시 경로가 없을 경우 무조건 `내 문서`를 열도록 하는 fallback 로직이 있다면 원인을 확인하여 수정한다.**

---

# 2. DB 삭제 시 실제 파일까지 삭제되는 문제

## 현재 문제

DB에 저장된 파일 정보를 삭제할 때 **실제 파일까지 삭제되는 문제가 발생**하고 있다.

이는 반드시 수정해야 한다.

## 핵심 원칙

### DB 데이터 삭제

```text
DB에서 레코드 삭제
```

와

### 실제 파일 삭제

```text
Windows 파일 시스템에서 파일 삭제
```

는 완전히 별개의 작업으로 취급해야 한다.

## 원하는 동작

DB에서 특정 파일 정보를 삭제하더라도:

```text
file_manager.db
    ↓
해당 레코드 삭제
```

만 수행한다.

실제 파일은:

```text
D:\새 폴더\example.jpg
```

그대로 유지되어야 한다.

## 절대로 수행하면 안 되는 동작

DB 레코드 삭제 과정에서 다음과 같은 파일 시스템 삭제가 실행되지 않도록 확인한다.

```python
os.remove(...)
```

```python
os.unlink(...)
```

```python
Path(...).unlink()
```

```python
shutil.rmtree(...)
```

또는 이와 동일한 실제 파일/폴더 삭제 로직.

## 원하는 구조

```text
DB 삭제
    ↓
DB 레코드만 DELETE
    ↓
완료

실제 파일
    ↓
그대로 유지
```

즉:

```text
DB 데이터 삭제 ≠ 실제 파일 삭제
```

이 원칙을 반드시 지킨다.

---

# 3. 파일 제목을 이용한 태깅 문제 수정

## 현재 문제

현재 파일에 일반 태그를 부착할 때 파일 내용을 분석하지 못하는 경우를 대비하여 **파일 이름(제목)을 이용하여 태그를 생성하는 방식**이 적용되어 있다.

예를 들어:

```text
음악목록.xlsx
```

이라는 파일이 있을 경우 파일 제목에 `음악`이라는 단어가 포함되어 있기 때문에:

```text
음악
```

이라는 태그가 붙을 수 있다.

이 방식 때문에 **검색 및 폴더 자동 분류의 정확도가 떨어지는 문제가 발생**하고 있다.

## 실제 발생하는 문제

예를 들어 사용자가 다음과 같은 문서를 가지고 있다고 가정한다.

```text
음악목록.xlsx
```

파일의 실제 내용은:

```text
음악 파일 목록을 정리한 문서
```

이다.

현재 제목 기반 태깅으로:

```text
음악목록.xlsx
    ↓
파일 제목 분석
    ↓
"음악" 태그 생성
```

이렇게 되면 검색에서:

```text
"음악 파일 찾아줘"
```

라고 사용자가 요청했을 때 시스템이:

```text
음악 태그
    ↓
음악 관련 파일 검색
    ↓
음악목록.xlsx
```

를 반환할 가능성이 생긴다.

하지만 사용자가 원하는 것은 **실제 음악 파일**이다.

예:

```text
song.mp3
music.wav
audio.flac
```

등의 음악 파일을 찾는 것이다.

즉:

```text
음악목록.xlsx
```

은 음악과 관련된 **문서**일 뿐 실제 음악 파일이 아니다.

---

# 4. 제목 기반 태깅 제거

위 문제를 해결하기 위해 **파일 제목을 이용하여 AI 태그를 생성하는 기능을 제거한다.**

기존:

```text
파일 내용 분석
    ↓
실패
    ↓
파일 제목 분석
    ↓
태그 생성
```

에서:

```text
파일 내용 분석
    ↓
실패
    ↓
파일 제목을 태그 생성에 사용하지 않음
```

으로 변경한다.

---

# 5. 태깅 우선순위

파일 태깅은 다음 원칙을 따른다.

```text
파일
 ↓
파일 내용 분석
 ↓
내용 분석 성공?
 ├─ YES → 내용 기반 AI 태그 생성
 │
 └─ NO
      ↓
    제목으로 태그 생성 ❌
      ↓
    확장자 기반 fallback 태그 사용
```

즉:

### 허용

```text
파일 내용
    ↓
AI 태그 생성
```

### 내용 분석 실패 시

```text
확장자
    ↓
기본 태그
```

### 금지

```text
파일 제목
    ↓
AI 태그 생성
```

---

# 6. 확장자 기반 태그는 유지

제목 기반 태깅을 제거한다고 해서 확장자 기반 태깅까지 제거하는 것은 아니다.

내용 분석이 불가능한 경우에는 기존 요구사항에 따라 확장자를 이용한 기본 태그를 적용한다.

예:

```text
song.mp3
    ↓
내용 분석 실패
    ↓
확장자 확인
    ↓
음악
```

```text
music.wav
    ↓
내용 분석 실패
    ↓
확장자 확인
    ↓
음악
```

```text
음악목록.xlsx
    ↓
내용 분석 실패
    ↓
확장자 확인
    ↓
문서
```

따라서 `음악목록.xlsx`라는 제목에 `음악`이 포함되어 있더라도 제목 때문에 `음악` 태그가 생성되지 않아야 한다.

---

# 7. 검색 및 자동 폴더 분류에 미치는 영향

이번 수정의 목적은 단순히 태그 하나를 제거하는 것이 아니다.

**검색 기능과 자동 폴더 분류의 정확도를 높이는 것이 핵심이다.**

예를 들어:

```text
음악목록.xlsx
```

은:

```text
문서
```

로 분류되어야 한다.

반면:

```text
song.mp3
music.mp3
audio.flac
```

등은 실제 음악 파일이므로:

```text
음악
```

으로 분류될 수 있어야 한다.

---

# 8. 기존 태깅 기능 수정 시 주의사항

기존 `setting_view`의 파일 태깅 기능을 수정할 때 다음 원칙을 지킨다.

```text
파일 내용
    ↓
내용 기반 태깅
```

이 부분은 유지한다.

단, 내용 분석에 실패했을 때:

```text
파일 제목
```

을 AI에 전달하여 태그를 생성하는 fallback 로직은 제거한다.

대신:

```text
파일 확장자
```

를 기준으로 기본 태그를 생성한다.

---

# 9. 수정 후 전체 동작

## 검색

```text
검색어 입력
    ↓
검색 결과 표시
    ↓
결과 클릭
    ↓
검색 결과의 실제 path 확인
    ↓
해당 경로 열기
```

더 이상 무조건 `내 문서`를 열어서는 안 된다.

## DB 삭제

```text
DB 삭제
    ↓
DB 레코드만 삭제
    ↓
실제 파일 유지
```

## 태깅

```text
파일
 ↓
내용 분석
 ↓
성공 ─────→ 내용 기반 AI 태그
 ↓ 실패
 ↓
파일 제목 사용 ❌
 ↓
확장자 확인
 ↓
확장자 기반 기본 태그
```

---

# 10. 최종 테스트 체크리스트

## 검색 결과 경로

- [ ] 검색 결과 클릭 시 `내 문서`가 열리지 않는가?
- [ ] 검색 결과의 실제 `path`를 사용하는가?
- [ ] 파일 검색 결과 클릭 시 해당 파일이 있는 폴더가 열리는가?
- [ ] 폴더 검색 결과 클릭 시 해당 폴더가 열리는가?
- [ ] 상대 경로/절대 경로 처리가 정상적인가?

## DB 삭제

- [ ] DB 레코드 삭제가 정상적으로 이루어지는가?
- [ ] DB 삭제 시 실제 파일이 삭제되지 않는가?
- [ ] 실제 폴더가 삭제되지 않는가?
- [ ] `os.remove`, `unlink`, `shutil.rmtree` 등의 파일 삭제 로직이 DB 삭제 과정에 연결되어 있지 않은가?

## 태깅

- [ ] 파일 내용 기반 태깅은 기존처럼 동작하는가?
- [ ] 내용 분석 실패 시 파일 제목을 태그 생성에 사용하지 않는가?
- [ ] 확장자 기반 fallback 태깅은 정상적으로 동작하는가?
- [ ] `음악목록.xlsx`가 제목 때문에 `음악` 태그를 받지 않는가?
- [ ] `음악목록.xlsx`는 확장자에 따라 `문서`로 처리되는가?
- [ ] 실제 `.mp3`, `.wav`, `.flac` 파일은 음악 파일로 처리될 수 있는가?
- [ ] 제목에 특정 키워드가 포함되어 있다는 이유만으로 해당 카테고리의 태그가 생성되지 않는가?

## 최종 확인

특히 다음 케이스를 반드시 테스트한다.

```text
파일명: 음악목록.xlsx
내용: 음악 파일 목록을 정리한 문서

결과:
태그 → 문서
검색 "음악 파일" → 해당 문서가 음악 파일로 잘못 검색되지 않음
```

그리고:

```text
파일명: song.mp3

결과:
태그 → 음악
검색 "음악 파일" → song.mp3 검색 가능
```

**핵심 원칙은 파일 제목이 아니라 실제 파일의 내용과 확장자를 기준으로 파일의 성격을 판단하는 것이다.**
